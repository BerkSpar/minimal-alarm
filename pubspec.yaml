name: minimal_alarm
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  path_provider: ^1.6.22
  hive: ^1.4.4+1
  day_night_time_picker: ^0.3.0+2
  lottie: ^0.7.0+1
  flutter_mobx: ^1.1.0+2
  mobx: ^1.2.1+3  
  flutter:
    sdk: flutter
  material_clock: ^0.0.2
  cupertino_icons: ^1.0.0

dev_dependencies:
  mobx_codegen: ^1.1.0+1
  build_runner: ^1.10.0    
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
  - assets/lotties/